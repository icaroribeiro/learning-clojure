(ns learning-clojure.learning-clojure-test
  (:require [clojure.test :refer [deftest is testing]]
            [learning-clojure.learning-clojure :as sut])) ; system under test

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
